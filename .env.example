# Server Configuration
PORT=3000

# Authentication
# Master API key for administrative operations (creating/managing API keys)
# Generate a secure random key for production use
MASTER_API_KEY=your-secure-master-key-here

# Example: Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# MCP Server Configurations
# GitHub MCP Server - Required for github server in config/servers.json
# Get a personal access token from https://github.com/settings/tokens
# Required scopes: repo, read:org, read:user, workflow (optional)
GITHUB_PERSONAL_ACCESS_TOKEN=your_github_personal_access_token_here

# Slack MCP Server - Required for slack server in config/servers.json
# Option 1: OAuth Token - Get from https://api.slack.com/apps
# Option 2: Stealth Mode - Use cookie-based authentication (xoxc-* token)
# For OAuth, required scopes: channels:history, channels:read, chat:write, groups:history, groups:read, im:history, im:read, mpim:history, mpim:read
SLACK_TOKEN=your_slack_token_here

# Discord MCP Server - Required for discord server in config/servers.json
# Get a bot token from https://discord.com/developers/applications
# Create a bot in Discord Developer Portal and copy the token
# Required permissions: Read Messages, Send Messages, Manage Messages, Read Message History
DISCORD_TOKEN=your_discord_bot_token_here
